import 'package:flutter/material.dart';

class CustomColumnContainResultWidget extends StatelessWidget {
  const CustomColumnContainResultWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
